#include <stdio.h>
#include <conio.h>
#include <math.h>
int main (){
    int n;
    char palavra[n];
    char fim[3];
    fim = ['f', 'i', 'm'];
    do {
        scanf("%d", &n);
        printf("%d", &n);
    } while (palavra[] === fim[]);
}
